目录结构：
  ./auto/: 脚本参数设定、脚本初始化
  ./conf/: 配置文件（yaml配置文件），可指定不同的环境，如prod、dev等
  ./util/: 通用的工具，jq、log、time、http、yaml文件解析等
  ./file-name: 主程序

不同业务：
  创建目录，e.g. service，将各个业务逻辑放入

想法：
  后续加入模块概念，将各个业务逻辑变成各个模块，按需要安装，类似 Nginx 按需安装模块
